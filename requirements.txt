# ============================================================================
# オフィス人物検出システム - 依存関係
# Office Person Detection System - Dependencies
# ============================================================================
# Python 3.10以上が必要です
# Python 3.10+ is required

# ----------------------------------------------------------------------------
# コア依存関係 (Core Dependencies)
# ----------------------------------------------------------------------------

# 深層学習フレームワーク
# PyTorch 2.0+ (MPS/CUDA/CPU対応)
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0
transformers>=4.30.0,<5.0.0
timm>=1.0.0,<2.0.0

# 数値計算・画像処理
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0,<5.0.0
Pillow>=10.0.0,<11.0.0
scikit-image>=0.21.0,<1.0.0

# データ処理
pandas>=2.0.0,<3.0.0
PyYAML>=6.0,<7.0.0

# 可視化
matplotlib>=3.7.0,<4.0.0
streamlit>=1.28.0,<2.0.0  # インタラクティブ可視化ツール用

# ユーティリティ
tqdm>=4.65.0,<5.0.0
python-Levenshtein>=0.21.0,<1.0.0
psutil>=5.9.0,<6.0.0  # パフォーマンス測定用

# 機械学習・評価
scikit-learn>=1.3.0,<2.0.0

# ----------------------------------------------------------------------------
# OCRエンジン (OCR Engines)
# ----------------------------------------------------------------------------
# 注意: 少なくとも1つのOCRエンジンが必要です
# Note: At least one OCR engine is required

# Tesseract OCR (Pythonラッパー)
# システムにTesseract本体をインストールする必要があります
# macOS: brew install tesseract tesseract-lang
# Ubuntu/Debian: sudo apt-get install tesseract-ocr tesseract-ocr-jpn
pytesseract>=0.3.10,<1.0.0

# PaddleOCR (オプション、高精度OCR、日本語対応)
# 注意: paddlepaddleが必要です（CPU版を推奨、Apple Silicon対応）
# GPU版を使用する場合は、CUDA環境に合わせて適切なバージョンを選択してください
# 参考: https://www.paddlepaddle.org.cn/install/quick?docurl=/documentation/docs/zh/install/pip/macos-pip.html
paddlepaddle>=2.5.0,<3.0.0; sys_platform != "win32"  # CPU版（Apple Silicon対応、Windows除外）
paddleocr>=2.7.0,<3.0.0

# EasyOCR (オプション、多言語OCR)
easyocr>=1.7.0,<2.0.0

# ----------------------------------------------------------------------------
# 開発・テスト用 (Development & Testing)
# ----------------------------------------------------------------------------
# 以下のパッケージは開発環境でのみ必要です
# The following packages are only required for development

# テストフレームワーク
pytest>=7.0.0,<9.0.0
pytest-cov>=4.0.0,<6.0.0  # カバレッジ測定用

# コード品質ツール
pre-commit>=3.5.0,<4.0.0  # Gitフック管理フレームワーク
ruff>=0.1.0,<1.0.0  # 高速なLint + Format + Import整理ツール（flake8/black/isortの代替）
mypy>=1.10.0,<2.0.0  # 静的型チェッカー
types-PyYAML>=6.0.12.20240808  # PyYAMLの型定義
# types-allはオプション（MyPy用の型定義パッケージ、必要に応じて個別にインストール）

# ----------------------------------------------------------------------------
# インストール方法 (Installation Instructions)
# ----------------------------------------------------------------------------
# 1. 仮想環境を作成:
#    python -m venv venv
#    source venv/bin/activate  # macOS/Linux
#    venv\Scripts\activate     # Windows
#
# 2. 依存関係をインストール:
#    pip install -r requirements.txt
#
# 3. OCRエンジンのシステム依存関係をインストール:
#    macOS: brew install tesseract tesseract-lang
#    Ubuntu/Debian: sudo apt-get install tesseract-ocr tesseract-ocr-jpn
#
# 4. 依存関係の確認:
#    python scripts/check_dependencies.py
